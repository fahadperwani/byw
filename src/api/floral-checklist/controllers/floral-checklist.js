'use strict';

/**
 * floral-checklist controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::floral-checklist.floral-checklist');
